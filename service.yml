---
-
  hosts: all
  gather_facts: true # this will make not to gather facts before running tasks

  tasks: 
    - name: bits
      state: stopped
      debug:
        msg: >
             this is for windows {{ stopping bits service }}
...